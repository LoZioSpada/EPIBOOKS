import { Alert } from 'react-bootstrap'
import styles from "./style.module.scss"

export default function Error () {
    <Alert variant='danger'>Error - Are you using your Bearer toker? Check it!!</Alert>
}